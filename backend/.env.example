# RetroPulse Backend Environment Configuration
# Copy this file to .env and update values as needed

# =============================================================================
# Server Configuration
# =============================================================================
NODE_ENV=development
PORT=3000

# =============================================================================
# MongoDB Configuration
# =============================================================================
# For local development with Docker:
#   docker run -d --name retropulse-mongo -p 27017:27017 \
#     -e MONGO_INITDB_ROOT_USERNAME=admin \
#     -e MONGO_INITDB_ROOT_PASSWORD=retropassword mongo:7
MONGODB_URL=mongodb://admin:retropassword@localhost:27017/?authSource=admin
MONGODB_DATABASE=retroboard

# =============================================================================
# Authentication Secrets
# =============================================================================
# IMPORTANT: Both secrets must be at least 16 characters for security
# The test suite expects these exact values for development:
#   COOKIE_SECRET=dev-cookie-secret-16chars
#   ADMIN_SECRET_KEY=dev-admin-secret-16chars
#
# For production, generate strong random secrets (32+ characters recommended):
#   openssl rand -base64 32

# Cookie signing secret (min 16 chars)
COOKIE_SECRET=dev-cookie-secret-16chars

# Admin API secret for test endpoints (min 16 chars)
# Used in X-Admin-Secret header for /v1/boards/:id/test/* endpoints
ADMIN_SECRET_KEY=dev-admin-secret-16chars

# =============================================================================
# URLs
# =============================================================================
# Frontend URL for CORS configuration
FRONTEND_URL=http://localhost:5173

# App URL used in shareable board links
APP_URL=http://localhost:3000
