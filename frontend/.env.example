# =============================================================================
# RetroPulse Frontend Environment Variables
# =============================================================================
# Copy this file to .env.local and update values as needed.
# Do not commit .env.local to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# Local Development (Vite)
# -----------------------------------------------------------------------------
# These are used when running `pnpm dev`
# The frontend connects to the backend API on port 3001 by default

VITE_API_URL=http://localhost:3001/v1
VITE_SOCKET_URL=http://localhost:3001

# Environment mode
VITE_ENV=development

# -----------------------------------------------------------------------------
# Docker Runtime (via env.js injection)
# -----------------------------------------------------------------------------
# When running in Docker, environment variables are injected at container
# startup via docker-entrypoint.sh, creating /env.js with window.__ENV__.
# The following are used by docker-compose.yml:
#
# API_URL=http://localhost:3000
# WS_URL=http://localhost:3000
# -----------------------------------------------------------------------------
